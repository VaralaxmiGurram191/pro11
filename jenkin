pipeline {
    agent any  

    stages {

stage('clone') { 
            steps {
                git ''
            }
        }
                
stage('Build') {
            steps {
                sh 'javac Helloworld.java'
            }
        }
stage('Test') {
steps{
sh'java helloworld'
}
}
}
}
